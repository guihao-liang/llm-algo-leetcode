# AGENTS.md

## Local Python Environment

- Use the repository-local virtual environment by default for Python commands: `.venv/bin/python`.
- When validating notebooks, examples, or scripts, prefer `.venv/bin/python` over the system `python` so dependencies and Python version match the project.
